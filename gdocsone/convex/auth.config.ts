/// no NOT rename the JWT template in clerk. Keep it 'convex'

export default {
    providers: [
        {
            domain: "https://possible-emu-71.clerk.accounts.dev",
            applicationID: "convex",
        }
    ]
}
